const fetchGitWorkflowImage = async () => {
  const imagePath = "";
  const gitDocuContainer = undefined;
  // Image in den gitDocuContainer einfüge (TIPP: .innerHTML)
  // Das Image sol einen vordefinierten Style hinterlegt
};
